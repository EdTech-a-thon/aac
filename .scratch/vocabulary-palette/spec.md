# Vocabulary Palette

Managers assign Button colors from a Vocabulary Palette (Fitzgerald-default at creation) or a custom hex, with unset/None as default. Palette edits and Button color bindings go through Change Sets; live state = Initial Snapshot + Applied Change Sets (ADR 0003, 0004).

See `issues/` for implementation tickets.
